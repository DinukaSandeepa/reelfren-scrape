{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for ReelFren Desktop App",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-sidecar",
    "shell:allow-open",
    "shell:allow-execute"
  ]
}
